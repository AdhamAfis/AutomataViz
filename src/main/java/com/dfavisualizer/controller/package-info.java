/**
 * Contains controller classes that coordinate between the UI and model.
 * 
 * This package includes classes that handle user input from the UI,
 * orchestrate operations on the model, and update the UI based on
 * the results. It acts as an intermediary layer between the UI and
 * the core model and algorithm packages.
 */
package com.dfavisualizer.controller; 